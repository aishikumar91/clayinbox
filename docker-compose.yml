services:
  emailbox:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/emailbox.db
    volumes:
      - emailbox-data:/data
    restart: unless-stopped

volumes:
  emailbox-data:
